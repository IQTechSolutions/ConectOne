@using IdentityModule.Domain.DataTransferObjects
@using Microsoft.Extensions.Localization
@using MudBlazor
@inject IStringLocalizer<AddRoleToBeManagedToParentRole> Localizer

<MudDialog>
    <TitleContent>
        @{
            <MudText Typo="Typo.h6">
                <MudIcon Icon="@Icons.Material.Filled.VerifiedUser" Class="mr-3 mb-n1" />
                @Localizer["ManageRoleTitle", _userDisplayName]
            </MudText>
        }
    </TitleContent>
    <DialogContent>
        @if (!_loaded)
        {
            <MudProgressLinear Color="Color.Primary" Indeterminate="true" Class="my-7" />
        }
        else
        {
            <MudSelect ToStringFunc="@roleConverter" T="RoleDto" Label='@Localizer["Roles"]'  @bind-Value="_userRole" AnchorOrigin="Origin.BottomCenter">
                @foreach (var state in _roles)
                {
                    <MudSelectItem T="RoleDto" Value="@state">@state.Name</MudSelectItem>
                }
            </MudSelect>
        }
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Filled" OnClick="Cancel">@Localizer["Cancel"]</MudButton>
        <MudButton Variant="Variant.Filled" OnClick="SubmitAsync" Color="Color.Success">@Localizer["ManageRole"]</MudButton>
    </DialogActions>
</MudDialog>